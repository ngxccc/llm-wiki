# MCP Usage Notes

To test the MCP server in VS Code, configure the workspace MCP server entry and run the app from the repository root.

Suggested query examples:

- "How do I start the MCP server?"
- "Where is the raw markdown data stored?"
- "What command runs Qdrant locally?"

The expected result is that the `search_wiki` tool returns a few relevant chunks from this repository.
